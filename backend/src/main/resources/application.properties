# Server Configuration
server.port=${PORT:8080}

# ===============================
# DATABASE CONFIGURATION - RENDER
# ===============================
spring.datasource.url=jdbc:postgresql://dpg-d69h7n56ubrc73aj1se0-a.oregon-postgres.render.com:5432/employeemanagement_6a8g?sslmode=require
spring.datasource.username=employeemanagement_6a8g_user
spring.datasource.password=tZ1cNAGCSNFyIdDmKBUFuTb11bGm5Tew
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================
# JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# CONNECTION POOL (HikariCP)
# ===============================
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=600000

# ===============================
# JWT Configuration
# ===============================
app.jwtSecret=${JWT_SECRET:mySecretKeyForJWTTokenGenerationAndValidation2023}
app.jwtExpirationMs=${JWT_EXPIRATION:86400000}

# ===============================
# ALLOW CIRCULAR REFERENCES (FIX FOR CURRENT ERROR)
# ===============================
spring.main.allow-circular-references=true

# ===============================
# Logging
# ===============================
logging.level.org.springframework.security=DEBUG
logging.level.com.employeemanagement=DEBUG

# ===============================
# CORS
# ===============================
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}